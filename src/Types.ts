
export interface sortedDataset{
    år: string[],
    kommune: string[],
    statistikkvariabel: string[],
    data: any[][][]
}

export interface options{
    år: string[],
    kommuner: string[],
    statistikkvariabler: string[]
}